# CONTEXT —— 领域词汇表

项目：**Mirror** —— Android ↔ Android 局域网双向投屏。
一个 APK、两个角色（发送端 / 接收端）；**传输与媒体栈复用现成 WebRTC 库**，不自创协议。

> 本文件只放词汇与关系。设计决策写进 `docs/adr/`，规格与进度写在 `docs/`。

## 角色与界面

| 词汇 | 含义 |
| --- | --- |
| **Sender（发送端）** | 采集本机屏幕与系统播放声音、交给媒体栈编码并推流的角色。 |
| **Receiver（接收端）** | 接收流、解码、渲染画面与声音，并按本地适配策略与视图变换显示的角色。 |
| **Role（角色）** | 一个进程在当前会话中扮演的身份；同一个 APK 可切换，一台设备可先后担任两种角色。 |
| **Tab（页签）** | 两个角色在同一个界面里以底部页签共存，随时切换 —— 不再有"首页"，也不再靠返回键导航。 |
| **DiscoveredDevice（被发现设备）** | 接收端通过广播被发送端看到的一条记录：设备名 + 地址 + 连接码。 |
| **ConnectCode（连接码）** | 6 位短码，用于人工核对"我要投给哪台设备"，并在信令阶段校验，避免误连。 |

## 发现与信令

| 词汇 | 含义 |
| --- | --- |
| **CastLink（投屏链接）** | 一段形如 `mirror://地址:端口?code=…&name=…` 的文本，二维码里装的就是它。它把"发现"从**广播**换成**人工传递**：扫码不建立任何连接，真正的连接仍是局域网直连。校验很严（scheme、端口、连接码），扫到无关二维码一律拒绝。 |
| **CastTarget（投屏目标）** | 从 CastLink 解析出的四项：地址、端口、连接码、设备名。扫码与广播搜索最终都归结成它 —— 后面的授权与建连**共用同一条路径**，扫码不是一个特例分支。 |
| **Beacon（发现报文）** | 接收端周期性 UDP 广播的小报文：连接码、信令端口、设备名。不加密，只承载建连所需的最小信息。 |
| **Signaling（信令）** | 交换会话描述与网络候选的通道；本项目走一条**临时 TCP 连接**，且**保持双向**（接收端的画质请求也从这里回去）。 |
| **SessionDescription（会话描述）** | 媒体协商文本（offer / answer）：编码格式、方向、参数。由媒体栈生成与解析，我们只负责搬运。 |
| **IceCandidate（网络候选）** | 本机可用于收流/发流的网络地址。局域网内双方只交换本机地址，不依赖任何外网服务。 |
| **PeerConnection（对等连接）** | 一次会话的媒体栈本体：负责编解码、加密、重传、拥塞控制、抖动缓冲、音画同步。 |
| **Session（会话）** | 从信令成功到任一方断开的完整生命周期。 |
| **QualityRequest（画质请求）** | 接收端 → 发送端：要求在**预算之内**换画质与帧率。它只带这两项，**不带码率**。 |
| **QualityState（画质状态）** | 发送端 → 接收端：回传当前生效的画质、帧率与码率上限，供接收端显示预算。 |

## 采集、编码与画质

| 词汇 | 含义 |
| --- | --- |
| **VideoTrack / AudioTrack（媒体轨道）** | 会话中单向传输的画面与声音通道；发送端发布、接收端订阅。 |
| **ScreenCapture（屏幕采集）** | 基于用户授权把本机屏幕变成视频源的过程。**虚拟屏尺寸使用屏幕真实尺寸**（仅把奇数抹为偶数）。 |
| **PlaybackCapture（声音内录）** | 采集本机其它应用正在播放的声音。受系统策略限制：未允许被捕获的应用会成为静音段，必须在界面说明。 |
| **NativeResolution（原生分辨率）** | 采集侧保持屏幕真实像素、不预先压缩尺寸的原则；它是后续 ROI 高清放大的前提。 |
| **QualityTier（画质档位）** | 编码输出的分辨率上限与码率上限，二者**联动**（原画 / 1080P / 720P / 480P / 360P）。只调编码输出，不碰采集。 |
| **FrameRateTier（帧率档位）** | 编码输出的帧率（30fps / 60fps / 跟随屏幕）。"跟随屏幕"以**设备刷新率**为上限 —— 采集的物理上限就是屏幕刷新率。 |
| **BitrateLimit（码率上限）** | 发送端为这条链路设定的带宽预算。**只有发送端能定**；画质与帧率都在它之内调。 |
| **EffectiveBitRate（生效码率）** | 三者取最小：码率上限、画质档位上限、按分辨率与帧率估算的值。 |
| **AutoQuality（自动画质）** | 发送端依据实测丢包率与往返时延自动升降档（拥塞连两次降、良好连六次升，且不会自动升到原画）。 |

## 接收端显示

| 词汇 | 含义 |
| --- | --- |
| **ContainerAspect（容器比例）** | 画面区外框的尺寸，由**屏幕可用区域**决定、**不跟随视频比例**；旋转屏幕时它会变。 |
| **FitMode（适配策略）** | 把视频放进容器的等比方式。**M1 只有 Fit**（完整可见，可能留黑边）：Compose 给 `AndroidView` 的是固定约束，而 WebRTC 的 `onMeasure` 在 EXACTLY 下会覆盖 `setScalingType`，所以 Fill（裁剪铺满）在这套布局里**无法生效**，干脆不提供。**永不变形，不存在拉伸**。 |
| **ViewTransform（视图变换）** | 用户的双指缩放与平移。基准是 Fit 后的画面（**1× = 完整显示**），旋转屏幕后复位。它是本地缩放的唯一事实源。 |
| **Viewport（小屏 / 全屏）** | 接收端的两种形态：小屏是全套功能所在处（含全屏/画质/帧率/比例/复位）；全屏是视频播放器形态，只留右下角退出。 |

## 连接方式

| 词汇 | 含义 |
| --- | --- |
| **WifiMode（同一 Wi-Fi）** | 默认连法：两台设备连同一个 AP，靠 UDP 广播发现彼此。 |
| **Hotspot（发送端热点）** | 备用连法：发送端用 `LocalOnlyHotspot` 开一个仅本地热点，接收端连上来。多数设备**不能同时连 Wi-Fi 又开热点**，所以它是兜底而非常态。 |
| **QrDirectConnect（扫码直连）** | 接收端把 CastLink 显示成二维码，发送端扫一下直接建连，**完全绕开 UDP 广播**。专治广播被路由器隔离、热点下搜不到、换网卡后 socket 绑错接口这三种情形；不需要任何服务器。它与"同一 Wi-Fi / 热点"是**正交**的：那两者说的是"数据走哪条链路"，它说的是"怎么找到对方"。 |

## 可观测性与验收

| 词汇 | 含义 |
| --- | --- |
| **LatencyBudget（延迟预算）** | 端到端目标延迟的分解额度：采集 + 编码 + 网络 + 抖动缓冲 + 解码 + 显示。改动任何一环都要先看它。 |
| **SessionStats（会话统计）** | 运行时可观测指标：端到端延迟、丢包率、码率、帧率、抖动缓冲深度、已解码帧数。由媒体栈统计派生。 |
| **DiagnosticLine（诊断行）** | 界面上常驻的一行状态文字。真机没有 adb，它是唯一的现场信息出口：**任何异常都必须出现在这里**。 |
| **AcceptanceRun（验收轮次）** | 一次真机验收：在同一对设备上量出端到端延迟、帧率、丢包、A-V 偏差与 10 分钟稳定性，作为后续优化的基线。 |

## 范围外（M1 明确不做）

| 词汇 | 说明 |
| --- | --- |
| **ROI（关注区域，M2）** | 只传放大区域以获得高清细节的能力；M1 的放大只是本地显示缩放，画质不随之提升。 |
| **TrustedReconnect / PairingGrant（M2）** | 长期设备身份与免 PIN 重连；M1 每次会话靠连接码校验。 |
| **InputInjector（M2）** | 反向控制（把接收端触摸注入发送端）；M1 的反向通道只承载画质请求，不注入输入事件。 |

## 关系

```
Sender ──(Beacon 发现)──▶ DiscoveredDevice ─┐
Sender ──(扫二维码 CastLink)──▶ CastTarget ──┴─▶ 一次投屏请求（授权 → 建连）
Sender ──(Signaling: offer/answer/candidate, 双向)──▶ Receiver
Sender ──(PeerConnection)──▶ Receiver
   │  VideoTrack ◀── ScreenCapture(MediaProjection)
   │  AudioTrack ◀── PlaybackCapture(AudioPlaybackCapture)
   │                          ▲
   │                          └── QualityRequest ◀── Receiver（预算之内）
   ▼
Receiver: PeerConnection ──▶ 画面渲染（ContainerAspect + FitMode + ViewTransform）+ 声音播放
```

- **唯一跨端接缝是信令通道**：上层只见"交换会话描述与候选、传递画质请求"，不碰 socket 细节。
- 媒体栈内部（编码、加密、重传、抖动缓冲、音画同步）**不是我们的代码**，不得在这上面叠自研协议。
- 采集、渲染、注入是适配层，是唯一允许碰 Android 平台 API 的地方。
- **带宽分工**：发送端定码率上限，接收端在预算内调画质与帧率。
