package com.whalecast.discovery

/**
 * 设备发现模块 —— **模块边界已就位，实现属于工单 09**。
 *
 * 计划内容：`NsdManager` mDNS 注册与浏览（服务类型 `_whalecast._tcp`）、
 * 手动 `IP:端口` 兜底入口，两条路径汇入同一个连接编排入口。
 */
internal object DiscoveryModulePlaceholder
